const ulElement = document.getElementById("formJs");
const formElement = document.getElementById("actualizarFormJs");


/*
function actualizarListaDeUsuarios(){
    ulElement.innerHTML = "";
    usuarios.forEach(function(usuario){
        let li = document.createElement('li');
        li.innerText = usuario.nombre + " " + usuario.apodo;
        ulElement.appendChild(li);
    });
}

formElement.addEventListener('submit', function(event){
    event.preventDefault();
    let nombreInput = formElement['nombre'].value;
    let apellidoInput = formElement['apodo'].value;
    let nuevoUsuario = {
        nombre: nombreInput,
        apodo: apodoInput
    };
    usuarios.push(nuevoUsuario);
    actualizarListaDeUsuarios();
});

actualizarListaDeUsuarios();

*/
